{
  "name": "tamanegiscope-runner",
  "image": "mcr.microsoft.com/devcontainers/python:1-3.12-bookworm",
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y --no-install-recommends tor && python -m pip install -r requirements.txt && python -m playwright install --with-deps chromium",
  "forwardPorts": [8000],
  "portsAttributes": {
    "8000": {
      "label": "TamanegiScope API",
      "onAutoForward": "notify"
    }
  }
}
