{
  "name": "tamanegiscope-runner",
  "build": { "dockerfile": "Dockerfile" },
  "postCreateCommand": "python -m pip install -r requirements.txt && python -m playwright install --with-deps chromium",
  "forwardPorts": [8000],
  "portsAttributes": {
    "8000": {
      "label": "TamanegiScope API",
      "onAutoForward": "notify"
    }
  }
}
